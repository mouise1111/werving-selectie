Band heading for marketing and overview pages — title left, quiet "see all" action right.

```jsx
<SectionHeading title="Uitgelichte vacatures" actionLabel="Bekijk alle vacatures" onAction={showAll} />
```

`size="hero"` is reserved for the one hero statement per page.
