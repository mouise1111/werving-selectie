Department / vakgebied entry point — used in a 4-column grid on the careers site.

```jsx
<CategoryTile label="Engineering" count={48} selected onClick={filterByDept} />
```

At most one tile is `selected` at a time; the inverted tile is the only dark element in a light band.
